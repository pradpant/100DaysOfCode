n=int(input())
c=0
if n>=2:
    print(2,end=' ')
for i in range (n):
    #print(t)
    for j in range(2,i):
        #print(j)
        if i%j==0:
            break
        else :
            c=c+1
            print(i,end=' ')
            break
#print(c+1)
